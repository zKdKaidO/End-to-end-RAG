# Debug UI Phase 11 — Debug UI

Date: 2026-08-19

One debug request renders one real `DebugTrace`: separate dense, lexical, and RRF tables; diagnostic signal semantics; lexical-mode enum; selected S1…Sn evidence; ranks; context budget; duplicate/drop/stop state; answerability; answer; citations; and provenance inspector.

Evaluation cases show immutable expected evidence beside actual retrieval/context/generation and deterministic diagnosis. Ad-hoc queries show no correctness judgment.

Real trace screenshot: `docs/verification/debug-ui-real-trace.png`.

Result: PASS.
