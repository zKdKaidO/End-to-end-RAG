# Copy to the untracked file named by CLOUDFLARE_TUNNEL_ENV_FILE on the App Node.
# This must be a NEW staging-only tunnel token. Never reuse the production token.
TUNNEL_TOKEN=replace-with-staging-only-cloudflare-tunnel-token
